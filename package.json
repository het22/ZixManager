{
  "name": "zixmanager",
  "version": "1.0.0",
  "description": "zix&dacx client management web application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "node server/app.js",
    "start": "concurrently \"npm run server\" \"cd client && npm start\""
  },
  "author": "het",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0"
  }
}
